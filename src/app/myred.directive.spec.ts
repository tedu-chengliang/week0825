import { MyredDirective } from './myred.directive';

describe('MyredDirective', () => {
  it('should create an instance', () => {
    const directive = new MyredDirective();
    expect(directive).toBeTruthy();
  });
});
